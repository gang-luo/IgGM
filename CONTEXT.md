# Context

分层扩散抗体设计（IgGM2）。抗体 FR 作半刚体、CDR 作全原子柔性，两者在同一去噪循环里同步生成；氨基酸类型从全原子几何解码，序列头不参与解码。

## Glossary

**FR（框架区）** — 抗体上被当作单一刚体处理的部分。其内部构象在生成过程中不变，只有整体位姿（旋转 + 平移）被加噪去噪。

**CDR** — 抗体上被当作柔性的 6 个 loop。以 atom14 全原子表示，在局部坐标系中做全原子扩散。

**位姿（pose）** — FR 刚体的整体旋转与平移二元组。等价于"抗体相对抗原的 docking 构型"，是本课题不预设 docking 的载体：位姿被加噪到近乎随机再去噪，因此 docking 是生成结果而非输入。

**规范帧（canonical frame）** — 位姿的参照系。由抗体自身几何（主成分 + N→CA / 首尾 CA 定向）唯一确定，使"抗体的旋转"成为有定义的量。

**体坐标系（body frame）** — 由带噪位姿定义的坐标系。抗原点云被换算到该系下，位姿预测即在此系内进行，保证等变性。

**局部坐标系（loop-local frame）** — 每个 CDR loop 由其左右 anchor 残基张成的坐标系。CDR 在该系内被预测，再经 FR 位姿换算回全局，保证 loop 与框架不断链。

**anchor 残基** — 紧邻 CDR loop 两端、归属 FR 的残基。随刚体运动，用于张成 loop-local frame。

**差值 θ-prediction** — FR 位姿的去噪参数化：网络预测"从带噪位姿到干净位姿的差量"而非位姿本身，使标签随每次加噪重采样而变，模型无法靠记忆位姿取巧。

**x0-prediction** — CDR 的去噪参数化：网络在 loop-local frame 内直接预测干净全原子坐标。

**atom14** — 每残基 14 个原子槽的定长全原子表示。不同氨基酸占用的真实原子数不同，空槽由虚拟原子填充。

**虚拟原子（ghost / marker）** — atom14 中未被真实原子占用的槽。当前方案把它们摆到 N/O 邻域作为类型标记（见"标记编码"）；另一路线是把它们塌到锚点上、不承载信息。

**辅助监督头（auxiliary supervision head）** — 只在训练时贡献梯度、推理时其输出被丢弃的预测头。本课题的序列头属此类：它的交叉熵参与训练（给主干一个类型信号，而 marker 摆位正是类型的几何编码），但类型解码只读全原子几何、从不读它的 logits。因此它**不构成**序列-结构不一致的风险——不一致的来源是"用 logits 定类型、用坐标定结构、两者可以打架"，而这条通路在本课题里不存在。须与"不设序列头"区分：本课题的主张是**序列头不参与解码**，不是不用它监督。

**标记编码（marker encoding）** — 当前的类型编码约定：虚拟原子在 N/O 锚原子附近（解码阈值内）的计数决定氨基酸种类。因此类型可从全原子几何直接解码，解码路径不经过序列头（见"辅助监督头"），规避序列-结构不一致。

**干净原子路线（clean-atom）** — 与标记编码互斥的备选：虚拟原子不承载类型信息，类型全靠真实原子的侧链形状/键长拓扑解码。

**n_real** — 某残基真实占用的原子槽数。它几乎唯一决定氨基酸类型，因此任何把 n_real 送进网络输入的路径都是类型泄露。

**true_len** — 某 CDR loop 的真实残基数。生成任务中 loop 长度本应未知（这正是"不预设长度"的卖点），因此送进网络即长度泄露。

**特征迭代精化** — 去噪主干在预测前反复多层更新 single/pair 特征，每层各自出一次位姿与 CDR 预测。

**交替去噪（alternating denoising）** — 逐层的建模流程：本层依当前 docking 位姿求最优 CDR 构象，下一层再依调整后的 CDR 重新 docking。因此位姿与 CDR 互为条件，缺一层就断一环。

**标签依赖 vs 输入依赖** — 两个必须分开的概念。位姿的**标签**（规范帧、`rota_orig`）应当只由 FR 原子定义，否则真值随 CDR 构象漂移、推理时无定义。位姿的**输入**则应当包含上一层预测的 CDR，这正是交替去噪要的闭环。"解耦"只针对标签，不针对输入。

**overfit 实验** — 用极少样本（当前 4 个复合体）训练，检验"模型是否有能力拟合"的健全性检查，而非泛化实验。

**定长阶段** — 当前阶段约定：各 CDR loop 的长度作为已知量给出，`true_len` 泄露被有意接受。不预设长度（含插入残基）是后续阶段的目标，不在当前实验的判据内。

**收缩（shrinkage）** — MSE 下的最优预测是目标的缩小版本（最优系数等于预测与目标的夹角余弦）。因此"预测幅度小于目标"是最优行为，不是欠拟合的证据。

**免费午餐（free lunch）** — 只靠学到正确的收缩系数、完全不改进方向就能拿到的那部分损失下降。用于划一条比"什么都没学到"更严的基线：损失降到免费午餐以内不算学会了几何。

**吸收率（absorption）** — 旋转监督的几何指标：`1 - θ(残差旋转) / θ(目标旋转)`。1 表示恰好施加了所需的修正，0 表示没动，负值表示改坏了。与损失值不同，它跨噪声桶可比，因为分母已归掉了该桶的目标幅度。

**归一化空间（x0_norm 空间）** — 网络实际输出所在的无量纲坐标系。物理坐标由 `x = x0_norm · cdr_scale + cdr_mu` 得到。损失在此空间计算时，回传梯度与数据尺度无关，这是 EDM 预处理的目的。

**坐标齐次项** — 可写成物理坐标的齐次函数的损失项（坐标 MSE、键长 MSE）。它们在归一化空间有精确对应形式，换算因子是 `cdr_scale` 的相应幂次。与之相对的**非齐次项**（含写死埃阈值的 smooth_lDDT、不依赖坐标的序列 CE）没有这样的换算，只能按实测梯度范数定权。

**梯度比** — 某损失项与基准项对同一参数的梯度范数之比。它而非损失值决定谁主导优化：有界饱和的项可以值大而梯度小。

**解码余量（anch_d）** — marker 到自己 N/O 锚原子的预测距离。真值恰为 0，解码阈值 1.0 Å，所以它而非 marker 的位置误差才是类型通道的判据：marker 跟着锚一起漂时位置误差大而解码仍然正确。**误判余量**是与之对偶的另一侧：真实原子被误数成 marker 所需的压缩量。因 atom14 槽 0–3 恒为真实主链原子（全部氨基酸 n_real ≥ 4），解码时整段跳过，最紧的余量由此从 C=O 的 0.23 Å 放宽到 CB 的约 1.4 Å。

**阈值死区** — smooth_lDDT 由 0.5/1/2/4 Å 四个 sigmoid 相加，回复力在相邻阈值之间（约 1.5 Å 和 3.0 Å 处）几乎归零。距离型监督因此非单调：误差落进死区的原子被"停住"，而坐标 MSE 的回复力随误差单调增长。这是"形状问题"而非"权重问题"的典型——缩放一个零点位置错了的力，零点还在原处，调权重无法修正。

**单步去噪 vs 反向采样** — 两种必须分开的评测。**单步**：在某个 t 上给干净结构加噪，网络预测一次干净结构，与真值比。**反向采样**：从纯噪声出发，把网络预测反复喂回自己走完整条轨迹。训练损失只监督单步，但生成质量取决于反向采样——单步准不蕴含反向采样不发散，因为误差沿轨迹累积。

**固定验证协议** — 为使不同 checkpoint 可比而冻结的评测配置：验证集样本、每个样本绑定的 timestep 集合、以及**存盘的噪声张量**（不是随机种子——种子的复现依赖 RNG 消耗顺序，改代码即失效）。

**表位条件（epitope conditioning）** — 抗原上的结合位点作为已知输入给出的设定（领域惯例，DiffAb / dyMEAN / 老 IgGM 皆如此）。必须与"预设位姿"分开：给定表位只说明"贴在哪一块"，抗体相对抗原的旋转与平移仍由生成得到，因此卖点 1 的准确措辞是**不预设相对位姿**。更强的一级是**表位自发现**（连结合位点也不给），是后续可拓展的目标而非当前设定。

**实际难度 vs 名义噪声档** — 必须分开的两个量。`t` 只固定噪声的**分布**，不固定单次实现的幅度，而后者的离散程度由通道**维度**决定：噪声范数服从 χ_d，CV 只随 d 变化、与 σ 无关。3 维的 trsl CV 恒为 0.422（实测 0.400），2772 维的 CDR CV 为 0.013（实测 0.016）。因此"固定 t"只对高维通道（CDR）意味着固定难度，对低维通道（trsl/rota 位姿）不然。这是扩散模型的固有性质而非加噪缺陷，FR 指标须按实际抽到的扰动幅度分桶。
